/**
 * Contract violation errors
 *
 * These errors are thrown to signal a programming error have
 * been detected during runtime. Do not rely on catching and
 * recover from these errors in normal program flow, since the
 * actual behavior will depend on the AssertiveProvider being
 * loaded.
 * 
 * @see org.valid4j.Assertive
 */
package org.valid4j.provider.errors;
